// Pega o botão do HTML
const botao = document.getElementById("meuBotao");

// Adiciona evento de clique
botao.addEventListener("click", () => {
  alert("Botão clicado!"); // Mostra alerta quando clicado
});